# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Clean2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Clean2
    pod 'SwiftyJSON',             '~> 4.0'
    pod 'Alamofire',              '~> 4.7'
    pod 'PromiseKit',             '~> 6.0'
    pod 'SDWebImage', '~> 4.0'
    pod 'SVProgressHUD'

  target 'Clean2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Clean2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
